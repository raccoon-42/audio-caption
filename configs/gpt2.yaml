seed: 42
prefix_len: 8
batch_size: 8
dropout: 0.3
weight_decay: 0.1
test_size: 0.1

data_dir: data/dataset
checkpoint_dir: checkpoints
results_dir: results

stage1:
  epochs: 30
  lr: 0.0005

stage2:
  epochs: 10
  projection_lr: 0.0001
  lm_lr: 0.000005
